"""
VoidRemote desktop GUI (PySide6). Requires the ``gui`` extra.

Not imported by the base ``voidremote`` package — import explicitly,
or run the ``voidremote-gui`` console script installed by
``pip install voidremote[gui]``.
"""
